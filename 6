// 3. Manipulação de Strings:
// Contar Letras:

let texto = prompt("digite um texto: ");

function contaLetras(texto) {
    let tiraEspaço = texto.replace(/ /g,"");
    console.log(tiraEspaço.length);

}
contaLetras(texto)
